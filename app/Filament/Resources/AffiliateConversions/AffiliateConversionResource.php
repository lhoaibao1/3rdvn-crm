<?php
namespace App\Filament\Resources\AffiliateConversions;
use App\Filament\Resources\AffiliateConversions\Pages\ListAffiliateConversions;use App\Models\AffiliateConversion;use App\Support\Permissions\RecordVisibility;use BackedEnum;use Filament\Resources\Resource;use Filament\Support\Icons\Heroicon;use Filament\Tables\Columns\TextColumn;use Filament\Tables\Filters\SelectFilter;use Filament\Tables\Table;use Illuminate\Database\Eloquent\Builder;
class AffiliateConversionResource extends Resource{protected static ?string $model=AffiliateConversion::class;protected static ?string $slug='applications/affiliate';protected static string|BackedEnum|null $navigationIcon=Heroicon::OutlinedCursorArrowRays;public static function getNavigationGroup():?string{return'Application';}public static function getNavigationLabel():string{return'Tiếp thị liên kết';}public static function getModelLabel():string{return'Kết quả chuyển đổi';}public static function getPluralModelLabel():string{return'Kết quả chuyển đổi';}public static function getNavigationSort():?int{return 50;}public static function shouldRegisterNavigation(array $parameters=[]):bool{return auth()->user()?->can('application.view')??false;}public static function canCreate():bool{return false;}public static function canEdit(mixed $record):bool{return false;}public static function table(Table $table):Table{return $table->heading('Kết quả chuyển đổi')->poll('15s')->defaultSort('conversion_time','desc')->columns([TextColumn::make('conversion_time')->label('Thời gian')->dateTime('H:i d/m/Y')->sortable(),TextColumn::make('conversion_id')->label('Mã chuyển đổi')->searchable()->copyable(),TextColumn::make('transaction_id')->label('Mã giao dịch')->searchable()->toggleable(),TextColumn::make('campaign_name')->label('Chiến dịch')->searchable()->placeholder('-'),TextColumn::make('product_name')->label('Sản phẩm')->searchable()->placeholder('-'),TextColumn::make('sale_amount')->label('Doanh số')->money('VND')->sortable(),TextColumn::make('publisher_payout')->label('Hoa hồng đối tác')->money('VND')->sortable()->visible(fn():bool=>auth()->user()?->hasRole('Admin')??false),TextColumn::make('conversion_status')->label('Trạng thái')->badge()->color(fn(?string $state):string=>match(strtolower((string)$state)){'success','approved'=>'success','cancelled','rejected'=>'danger',default=>'warning'}),TextColumn::make('createdBy.name')->label('Nhân viên')->searchable()->placeholder(fn(AffiliateConversion $record):string=>$record->aff_sub1?:'-'),TextColumn::make('aff_sub1')->label('Mã NV')->searchable()->toggleable(),TextColumn::make('updated_at')->label('Cập nhật')->since()->sortable()])->filters([SelectFilter::make('conversion_status')->label('Trạng thái')->options(fn():array=>AffiliateConversion::query()->whereNotNull('conversion_status')->distinct()->orderBy('conversion_status')->pluck('conversion_status','conversion_status')->all())]);}public static function getEloquentQuery():Builder{return RecordVisibility::applyUserScope(parent::getEloquentQuery()->with('createdBy'),auth()->user(),'created_by_id','createdBy');}public static function getPages():array{return['index'=>ListAffiliateConversions::route('/')];}}
